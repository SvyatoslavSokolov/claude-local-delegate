# TODO: аудит качества библиотеки

План составлен по статическому аудиту архитектуры, логики и согласованности API. Исходный код не изменён; тесты аудита не запускались.

## P2 — исправить в первую очередь

- [x] **Корректно обрабатывать `blocked` в `get_delegate_result`.** При `wait_seconds=0` возвращать статус ожидания ввода, а не последнее сообщение как финальный результат. Добавить проверки для `blocked` с нулевым и ненулевым ожиданием. (`server.py:1734`)
- [x] **Безопасно разбирать числовые переменные окружения.** Вынести парсинг в единый валидатор с fallback на значения по умолчанию; некорректная настройка не должна ломать импорт MCP. (`server.py:351` и соседние настройки)
- [x] **Убрать побочные эффекты из GET dashboard.** Чтение списка задач не должно менять статусы и писать события в SQLite; автоматическую сверку вынести в отдельный reaper/action. (`dashboard.py:119`)
- [x] **Валидировать входные данные POST.** Некорректные `Content-Length` и `max_age_hours` должны давать HTTP 400, а не необработанное исключение/500. (`dashboard.py:1983`)
- [x] **Уточнить расчёт готовности кластера.** Разделить состояние vLLM и gateway; `overall_healthy` не должен быть истинным при недоступном обязательном компоненте. Добавить проверки отказа каждого компонента. (`cluster_telemetry.py:131`)
- [x] **Синхронизировать документацию с ограничением параллелизма.** Код намеренно больше не отказывает при превышении шести запусков, но brief всё ещё обещает overload refusal. Либо вернуть enforcement, либо явно описать advisory-поведение и его риск. (`docs/ARCHITECT_BRIEF.md:17`, `coordination_runtime.py:143`)

## P3 — следующий этап

- [x] **Разделить публичный интерфейс запуска и dashboard.** Убрать прямой вызов приватных функций `server` из dashboard; оставить один публичный adapter-контракт для запуска, статуса и результата. (`dashboard.py:244`)
- [x] **Оптимизировать `Board.sync`.** Фильтровать задачи по проекту на уровне SQL и добавить индекс, чтобы синхронизация не десериализовала всю историю. (`coordination.py:151`)
- [x] **Удалить затенённое определение `main` в AGY-адаптере.** Оставить одну реализацию и проверить CLI-запуск. (`adapters/agy/agy_delegate.py:332`, `:588`)

## Приёмка

- [x] Добавлены регрессионные тесты для каждого исправленного дефекта.
- [x] Проверены сценарии конкурентных запусков, отмены, таймаута и рассинхронизации roster/транскрипта.
- [x] Запущен полный набор тестов проекта; результат и ограничения проверки отражены в документации.

## Ограничения аудита

- Проверка была статической: внешние `claude --bg`, AGY и vLLM не запускались.
- Гипотезы о flock, межпроцессной конкуренции и полном verified-loop требуют отдельного динамического тестирования.
- Утверждение о проблеме 4-пробельных YAML-отступов в первоначальном отчёте отклонено после сверки с кодом.
